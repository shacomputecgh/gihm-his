{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Main window capability — core + updater (custom commands need no ACL entry).",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "updater:default",
    "sql:default"
  ]
}
